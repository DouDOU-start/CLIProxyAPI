# Local database bootstrap configuration.
# All server settings, authentication accounts, model prices, and usage statistics
# are stored in PostgreSQL and managed through the management console.
postgresql:
  dsn: "postgresql://cliproxy:change-this-password@postgres:5432/cliproxy?sslmode=disable"
  schema: "public"
